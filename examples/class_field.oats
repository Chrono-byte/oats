export class Point {
    constructor(x: number, y: number) {
        this.x = x;
        this.y = y;
    }

    sum(): number {
        return this.x + this.y;
    }
}

export function main(): void {
    let p = Point_ctor(1.0, 2.0);
    let v = 0.0;
    return;
}
