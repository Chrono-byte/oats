export function fibonacci(n: number): number {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

export function main(): number {
    for (let mut i: number = 1; i <= 20; i = i + 1) {
        let fib_i: number = fibonacci(i);
        print_f64(fib_i);
    }
    return 0;
}
