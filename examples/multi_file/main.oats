// main.oats - Entry point that imports from math.oats

import { add, multiply } from "./math";

export function main(): number {
    const x: number = add(3, 4);
    const y: number = multiply(2, 5);
    const result = x + y;
    println(result);
    return 0;
}
