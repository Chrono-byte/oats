// Test object literal without shorthand

export function main(): number {
    let point: { x: number, y: number } = { x: 100, y: 200 };
    
    println("Object test:");
    println(point.x);
    println(point.y);
    
    return 0;
}
